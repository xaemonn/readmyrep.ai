Goal
read all separate raw CSV files and process them and 
merge them into a single data frame.

accelerometer data then gyroscope data- alternate in data/raw/metamotion

accelerometer- its in g(g force)
gyro- its in degree/sec

data collected by-
n=5 with different years of exp in fitness

a participant gets ready to do some squats. he/she has watch on, we press record on watch, participants squats and we export data.
oneheavy set and one light set

acceleration is how speed is changing(check notes)
gyroscope tells us about the rotation

we use supervised learning here we have both structured and unstructured data and we create multi classfication model

REST data- where participants were resting during the sets
(either sitting/walking etc)

A-bench-heavy file names contain the labels for the data
and 2 separate files for each(acc, gyro)- so we have to transform this data-> data processing

